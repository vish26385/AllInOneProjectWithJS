<div class="table-responsive">
    
    <table id="saleDetailTable" class="table table-bordered table-hover align-middle">
        <thead class="table-light text-center">
            <tr>
                <th>Item Name</th>
                <th>Qty</th>
                <th>Rate</th>
                <th>Amount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <input type="hidden" class="saleDetailId" />
                <input type="hidden" class="saleMasterId" />

                <td style="padding: 8px; width: 20%;">
                    <select class="itemId form-select">
                        <option value="0">-- Select Item --</option>
                    </select>
                </td>
                <td style="padding: 8px; width: 20%;">
                    <input type="number" class="itemQty form-control text-center" />
                </td>
                <td style="padding: 8px; width: 20%;">
                    <input type="number" readonly class="itemPrice form-control text-center" />
                </td>
                <td style="padding: 8px; width: 20%;">
                    <input type="number" readonly class="itemAmount form-control text-center" />
                </td>
                <td style="padding: 8px; width: 20%;" class="text-center">
                    <div class="btn-group" role="group">
                        <button type="button" class="addNewRow btn btn-sm btn-success rounded-2" onclick="addNewRow()" style="margin-right:5px;">
                            <i class="bi bi-plus-circle me-1"></i> Add
                        </button>
                        <button type="button" class="deleteRow btn btn-sm btn-danger rounded-2" onclick="deleteRow(this)">
                            <i class="bi bi-trash me-1"></i> Delete
                        </button>
                    </div>
                </td>
                @* <td style="padding: 8px; width: 20%;" class="text-center">
                    <button type="button" class="addNewRow" onclick="addNewRow()"
                            style="padding: 6px 12px; background: #4CAF50; color: #fff; border: none; border-radius: 5px; cursor: pointer; font-weight: 600; margin-right: 5px;">
                        + Add
                    </button>
                    <button type="button" class="deleteRow" onclick="deleteRow(this)"
                            style="padding: 6px 12px; background: #f44336; color: #fff; border: none; border-radius: 5px; cursor: pointer; font-weight: 600;">
                        ✕ Delete
                    </button>
                </td> *@
            </tr>
        </tbody>
    </table>
</div>

<style>
    #saleDetailTable tbody tr:hover {
        background-color: #f9f9f9;
    }

    /* #saleDetailTable th {
        font-weight: 600;
        color: #444;
    } */

    #saleDetailTable td input:read-only {
        cursor: not-allowed;
    }

    #saleDetailTable button:hover {
        opacity: 0.9;
    }

    @@media (max-width: 768px) {
        #saleDetailTable, #saleDetailTable thead, #saleDetailTable tbody, #saleDetailTable th, #saleDetailTable td, #saleDetailTable tr {
            display: block;
            width: 100%;
        }

            #saleDetailTable thead tr {
                display: none;
            }

            #saleDetailTable td {
                padding: 10px;
                text-align: right;
                position: relative;
            }

                #saleDetailTable td::before {
                    content: attr(data-label);
                    position: absolute;
                    left: 10px;
                    font-weight: 600;
                    color: #555;
                }
    }
</style>
